const login = () => {
    const usernameInput = document.querySelector("#email").value;
    const passwordInput = document.querySelector("#password").value;

    const dataLocalStorage = JSON.parse(localStorage.getItem);
}